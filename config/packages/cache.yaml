framework:
    cache:
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'

        pools:
            auth.token.cache:
                adapter: cache.app
                default_lifetime: 3600
