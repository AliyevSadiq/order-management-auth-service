nelmio_api_doc:
    documentation:
        info:
            title: Auth Service API
            description: Authentication and authorization service - register, login, JWT token management
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api
            - ^/health/auth
